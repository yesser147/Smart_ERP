server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Angular routes (/dashboard, /auth/login...) are handled by the app
    location / {
        try_files $uri $uri/ /index.html;
    }
}
