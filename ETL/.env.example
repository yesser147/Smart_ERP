# Copy to ETL/.env and fill in.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=smart_erp
DB_USER=postgres
DB_PASSWORD=change-me

# Optional: where the raw files are (defaults: ./sample_data, ./output, ./reports)
# ETL_INPUT_DIR=./sample_data
# ETL_OUTPUT_DIR=./output
# RESUME_CSV=./sample_data/Resume/Resume.csv
# RESUME_PDF_DIR=./sample_data/data/data

# CV enrichment (enrich_cv_storage.py)
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=change-me
